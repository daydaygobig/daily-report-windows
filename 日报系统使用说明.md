---
created: 2026-07-16
updated: 2026-07-19
status: active（进行中）
---

# 日报系统使用说明（Windows）

这份说明是写给不懂编程的人看的。里面出现的英文名称，都是页面上真实存在的按钮或输入框；每个配置项后面都会说明它是什么意思、该怎么填。

第一次使用时，建议严格按照本文顺序操作。不要一上来就创建任务，否则很容易因为前面的账号还没配好而失败。

## 一、先用大白话认识这套系统

这套系统里有几个经常出现的名字：

- **WeFlow**：负责把微信群里的聊天内容提供给日报系统，相当于“取材料的人”。
- **模型**：负责阅读群聊并写日报，相当于“写稿的人”。
- **提示词**：告诉模型应该怎么写，相当于“写作要求”。
- **任务**：决定处理哪个群、生成日报还是话题卡片，相当于“要做什么”。
- **作业**：决定什么时候做、处理哪段时间、文件保存到哪里，相当于“什么时候做、做完放哪里”。
- **Webhook**：把结果发到飞书群，相当于“飞书收件地址”。
- **GitHub**：把 HTML 日报放到网上，生成可以打开的网页链接。
- **ima 知识库**：把日报文件自动放进 ima，方便以后检索和提问。

最重要的一句话是：

> **任务决定内容，作业决定时间和去向。一个任务下面可以有多个作业。**

例如，“星火群日报”可以是一个任务；它下面可以有“每天早上发日报”和“每周一发周报”两个作业。

## 二、第一次配置的正确顺序

建议按下面顺序配置：

1. 启动日报系统。
2. 配置 WeFlow，让系统能看到群聊。
3. 配置模型，让系统能生成内容。
4. 配置飞书 Webhook，让结果能发到飞书群。
5. 配置提示词。
6. 创建任务，选择群聊和模型。
7. 在任务下面创建作业，设置时间和保存位置。
8. 手动执行一次，确认整套流程正常。
9. 如果需要，再配置 GitHub 和 ima；这两项不是必需的。

## 三、第一次启动日报系统

先打开 WeFlow，并让它保持运行。

### 1. 本机配置文件

第一次启动时，脚本会自动根据 `.env.example` 生成本机配置文件 `.env`。一般不用自己处理，也不用打开 `.env` 改内容。

`.env` 只放在自己的电脑里，不要发给别人。WeFlow Token、模型 API、飞书、GitHub 和 ima 这些配置，后面都在日报系统网页里填写，不在 `.env` 里填。

### 2. 运行一键启动脚本

1. 在 Windows 文件资源管理器中打开日报系统的项目文件夹。
2. 双击项目最外层的 `启动日报系统.cmd`。
3. 系统会自动弹出运行窗口。第一次启动可能要等几分钟，运行期间不要关闭这个窗口。

第一次启动时，系统会自动检查电脑基础运行环境和项目环境依赖：

- Python 3.11 或更高版本
- Node.js 18 或更高版本
- Poetry
- pnpm

如果缺少 Poetry 或 pnpm，脚本会尝试自动补齐。如果缺少 Python 或 Node.js，脚本会优先尝试通过 Windows 自带的 winget 自动安装；如果自动安装失败，窗口会给出明确提示。不要关闭窗口，把报错截图发给帮你的人即可。

启动窗口里会看到类似这样的提示：

```text
[start_all] 第 1 步：检查电脑基础运行环境（Python/Node.js/Poetry/pnpm）。
[start_all] 检查结果：电脑基础运行环境原本已齐全，本次没有安装新软件。
[start_all] 第 2 步：检查后端环境依赖（项目里的 Python 包）。
[start_all] 检查结果：本地后端环境依赖已齐全，本次没有安装新依赖。
[start_all] 第 3 步：检查网页环境依赖（项目里的网页包）。
[start_all] 检查结果：本地网页环境依赖已齐全，本次没有安装新依赖。
```

如果确实补装了依赖，也会明确提示“已自动补齐”，不会静默失败。

### 3. 打开日报页面

用 Edge 或 Chrome 打开启动窗口最后显示的地址，例如：

[http://127.0.0.1:5173](http://127.0.0.1:5173)

正常情况下使用上面这个地址。如果端口已被其他软件占用，启动脚本会自动换一个可用端口，比如 `5174`、`5175`。请以窗口最后显示的“请打开这个日报页面”为准。

能看到“群聊日报智能助手”页面，就说明系统已经启动。

### 4. 以后每次怎么启动

1. 先打开 WeFlow。
2. 双击项目最外层的 `启动日报系统.cmd`。
3. 保持脚本弹出的运行窗口打开。
4. 打开启动窗口最后显示的日报页面地址。通常是 `http://127.0.0.1:5173`。

如果日报系统原本就在运行，脚本会提示：

```text
[start_all] 检测结果：日报系统原本就在运行，本次没有重复启动。
[start_all] 请直接在浏览器访问 http://127.0.0.1:5173
```

### 5. 怎么停止

回到一键启动脚本弹出的运行窗口，同时按下 `Ctrl + C`，然后关闭窗口。

也可以直接关闭这个启动窗口。窗口关闭后，日报系统的后端和前端服务会一起停止。
## 四、配置 WeFlow：让系统能读取群聊

### 1. 先在 WeFlow 里打开 API 服务

1. 打开 WeFlow。
2. 进入“设置 → API 服务”。
3. “监听地址”保持 `127.0.0.1`。
4. “服务端口”保持 `5031`。
5. 找到“Access Token（鉴权凭证）”。如果这里是空的，点击“随机生成”。
6. 复制 Access Token，先放在自己能找到的地方，不要发给别人。
7. 找到“API 地址”，点击右边的复制按钮。默认复制出来的是 `http://127.0.0.1:5031`。
8. 打开页面里的 API 服务开关，让服务保持运行。

### 2. 在日报系统里填写 WeFlow

进入左侧的“系统设置”，找到“聊天记录接口”。

| 页面上的名称 | 大白话解释 | 建议怎么填 |
|---|---|---|
| 聊天记录接口 | 选择从哪里读取群聊 | 选择 `WeFlow` |
| WeFlow 地址 | 刚才在 WeFlow“API 服务”页面复制的 API 地址 | 直接粘贴，默认是 `http://127.0.0.1:5031` |
| WeFlow Token | 刚才在 WeFlow“API 服务”页面复制的 Access Token | 直接粘贴，不要加空格或引号 |
| 每页拉取条数 | 每次向 WeFlow 要多少条消息 | 保持 `1000` 即可 |
| 单页超时时间 | 消息很多时，最多等多久 | 保持 `180` 秒即可 |
| 空页重试次数 | 偶尔没拿到内容时，再试几次 | 保持 `2` 即可 |

填写完以后：

1. 点击“测试 WeFlow”。
2. 显示测试通过后，点击“保存配置”。

以后再打开这个页面时，Token 输入框可能是空的，但下面显示“已保存 Token”。这是正常的，系统是故意不把完整 Token 直接显示出来。

### 3. WeFlow 连接成功，但任务里找不到群

按下面顺序检查：

1. WeFlow 是否保持打开。
2. WeFlow 的 API 服务是否打开。
3. WeFlow 本身是否已经能看到这个群的内容。
4. 日报系统的 WeFlow 测试是否通过。
5. 在任务的“群聊名称”输入框里输入群名关键词搜索。

## 五、配置模型 API：让系统能写日报

### 1. 先向模型服务商要四样东西

不管使用哪一家模型，都要准备：

1. **API Key**：模型公司的通行密码。
2. **模型 ID**：具体使用哪个模型的名称。
3. **完整接口地址**：模型公司提供的请求地址。
4. **请求标准**：这个接口使用 OpenAI、Gemini 还是 Anthropic 的格式。

如果服务商只给了一个网页聊天账号，没有给 API Key，就不能直接填进这里。

### 2. 在日报系统里新增模型

进入左侧“模型”，点击“新增模型”。

| 页面上的名称 | 大白话解释 | 建议怎么填 |
|---|---|---|
| 模型厂商 | 给这套配置起一个容易认的名字 | 例如“DeepSeek 日报模型”或“火山日报模型” |
| 模型 ID | 真正要调用的模型名称 | 原样填写服务商给你的 model 值；不确定就问服务商 |
| 拉取模型列表 | 尝试自动列出账号能用的模型 | 先填接口地址和 API Key，再点；拉取失败也可以手填模型 ID |
| Base URL | 模型的完整接口地址 | OpenAI 兼容接口通常要完整写到 `/chat/completions`，以服务商说明为准 |
| 请求标准 | 告诉系统应该用哪种方式和模型说话 | 大多数兼容接口选 `OpenAI`；Gemini 选 `Gemini`；Claude 原生接口选 `Anthropic` |
| API Key | 模型服务商给你的密钥 | 原样粘贴，不要加引号或空格 |
| 最大输出 token | 最多允许模型写多长 | 不知道就先留空；服务商有要求时按服务商填写 |
| 最大输出参数名 | 模型公司使用的“长度限制”字段名 | 一般保持 `max_tokens`；报参数错误时再按服务商要求改 |
| 温度 | 内容更稳定还是更自由 | 日报建议 `0.2` 到 `0.5`；不知道就留空 |
| Top P | 另一种控制回答变化程度的设置 | 普通用户建议留空 |
| 思考级别 | 是否让支持推理的模型多想一会儿 | 不确定就选“无”；模型明确支持时再选 `medium` 或 `high` |
| 高级配置 | 给特殊模型添加额外要求 | 普通用户不要填写 |

填写完成后，先点击“连通性测试”。测试成功后再点击“保存”。

### 3. 主模型和备用模型是什么意思

创建任务时，可以设置“模型执行顺序”：

- 第一行是主模型，系统先用它。
- 后面的模型是备用模型，主模型失败后才会使用。
- “最多执行次数”表示这个模型最多试几次。

第一次使用时，先只选一个主模型即可。等主流程跑通以后，再添加备用模型。

## 六、配置飞书 Webhook：把结果发到飞书群

Webhook 可以理解成一个“只能往指定飞书群送消息的收件地址”。一个飞书群需要单独创建一个自定义机器人；同一个 Webhook 不会自动发到其他群。

### 1. 在飞书群里创建自定义机器人

1. 打开要接收日报的飞书群。
2. 点击群右上角的“更多”或三个点。
3. 进入“设置”或“群设置”。
4. 找到“群机器人”。
5. 点击“添加机器人”，选择“自定义机器人”。
6. 给机器人起名，例如“群聊日报”。
7. 按飞书页面要求完成安全设置。
8. 创建完成后，复制飞书给出的 Webhook 地址。

Webhook 地址通常以这段内容开头：

```text
https://open.feishu.cn/open-apis/bot/v2/hook/
```

如果飞书要求设置“关键词”，可以先使用“日报”，同时把任务或作业名称写成包含“日报”的名字。测试推送失败时，要检查是不是被飞书的关键词规则拦住了。

### 2. 在日报系统里新增 Webhook

进入左侧“Webhook”，点击“新增 Webhook”。

| 页面上的名称 | 大白话解释 | 建议怎么填 |
|---|---|---|
| Webhook 名称 | 给这个飞书群起一个好认的名字 | 例如“星火群-日报推送” |
| Webhook URL | 飞书刚才给你的收件地址 | 完整粘贴，不要截断 |
| Headers (JSON) | 给特殊接口附加额外信息 | 飞书自定义机器人一般留空 |
| 卡片模式 | 飞书里用什么样式显示文字 | 保持“Markdown 卡片” |
| 自定义标题 / 副标题 / 主题色 | 是否自己设置飞书卡片顶部样式 | 不需要就关闭；打开后再填标题和颜色 |
| 卡片标题 | 飞书消息最上面的标题 | 可写 `{task_name} · {job_name}`；也可留空使用作业名称 |
| 卡片副标题 | 标题下面的小字 | 可不填 |
| 主题色 | 卡片顶部颜色 | 按喜好选择，不影响内容 |
| 图片渲染引擎 | 话题卡片转成图片时使用哪种方式 | 保持 `Satori` |
| 飞书应用 App ID | 上传话题卡片图片时使用的应用身份 | 只发文字时留空 |
| 飞书应用 App Secret | 上面这个应用的密码 | 只发文字时留空 |

先保存 Webhook。普通文字日报不需要填写 App ID 和 App Secret。

### 3. 如果要发送话题卡片图片

只有开启“话题卡片图片推送”时，才需要 App ID 和 App Secret。

1. 打开[飞书开放平台](https://open.feishu.cn/)，进入开发者后台。
2. 创建一个“企业自建应用”。
3. 在应用能力里添加“机器人”能力。
4. 在权限管理里开通“获取与上传图片或文件资源”。
5. 按飞书后台要求创建版本并发布，让权限生效。
6. 在“基础信息 → 凭证与基础信息”里复制 App ID 和 App Secret。
7. 回到日报系统，编辑刚才保存的 Webhook。
8. 填入 App ID 和 App Secret，保存。
9. 再次编辑这个 Webhook，先点“测试图片上传”，再点“测试图片推送”。

App ID 和 App Secret 最好来自与目标飞书群相同的企业。App Secret 只显示或使用于配置，不要发给别人。

## 七、配置提示词：告诉模型日报应该怎么写

进入左侧“提示词管理”，点击“新建提示词”。

| 页面上的名称 | 大白话解释 | 建议怎么填 |
|---|---|---|
| 提示词名称 | 给这套写作要求起名 | 例如“星火群每日简报” |
| 提示词内容 | 具体告诉模型要写什么、怎么写 | 写清栏目、长度、语气、不要做什么 |
| 备注 | 方便自己以后区分用途 | 可写“用于每天早上 8 点的日报” |

一份简单提示词可以这样写：

```text
请根据群聊内容生成一份中文日报。
先写今日重点，再写主要讨论、重要结论和待办事项。
没有在群聊中出现的信息不要编造。
语言简洁，让没有参加群聊的人也能看懂。
```

提示词模板保存后，创建任务时可以直接选择。选择后仍然可以在当前任务里修改内容，不会影响原来的模板。

“自定义系统提示词”属于高级用法。第一次使用建议关闭，先把普通提示词跑通。

## 八、日报、话题卡片、数据导出有什么区别

| 类型 | 它会做什么 | 是否使用模型 | 适合什么情况 |
|---|---|---|---|
| 日报 | 阅读群聊并写成一篇完整总结 | 会 | 每日简报、周报、会议总结 |
| 话题卡片 | 从群聊中挑出多个值得看的话题，分别做成卡片 | 会 | 飞书群内快速浏览、精选话题分享 |
| 数据导出 | 只读取和整理数据，不让模型写文章 | 不会 | 统计群成员发言数量、保存表格、减少模型费用 |

### 日报

日报适合长一些的总结，可以：

- 推送到飞书。
- 保存模型返回的 Markdown 或 TXT 文件。
- 保存为本地 HTML 网页。
- 上传到 GitHub Pages。
- 同步到 ima 知识库。

### 话题卡片

话题卡片不是一篇长日报，而是把多个话题拆开显示。作业里还可以设置：

- 逐话题发送：一个话题发一条飞书消息。
- 合并发送：多个话题放在同一条飞书消息里。
- 自动：话题少时分开发，话题多时自动合并。
- 图片推送：把话题卡片做成 PNG 图片后再发到飞书。

### 数据导出

数据导出不会调用模型，所以不会生成 AI 总结。最常见的用法是打开“导出群消息统计”，生成 Markdown、CSV 或 Excel 文件。

如果只想知道“谁发了多少条消息”，应该选数据导出，不需要配置模型。

## 九、创建任务：决定处理哪个群、生成什么内容

进入左侧“任务&作业”，点击“新增任务”。

| 页面上的名称 | 大白话解释 | 建议怎么填 |
|---|---|---|
| 任务名称 | 这件事叫什么 | 例如“星火群日报” |
| 任务类型 | 要生成日报、话题卡片还是只导出数据 | 按上一章选择 |
| 话题样式配置 | 四类话题分别用什么版式和颜色 | 只在话题卡片任务中出现；先用默认值 |
| 提示词模板 | 选择提前保存好的写作要求 | 日报和话题卡片必须选；数据导出不需要 |
| 提示词内容 | 当前任务真正发送给模型的要求 | 选择模板后检查一遍，可针对当前群修改 |
| 自定义系统提示词 | 给模型增加更底层的固定要求 | 普通用户先关闭 |
| 附加群消息总数 | 把消息总数一并告诉模型 | 需要模型在日报里引用消息数量时再打开 |
| 模型执行顺序 | 主模型和备用模型的使用顺序 | 第一次只选一个主模型 |
| 群聊名称 | 从 WeFlow 里选择要处理的群 | 支持搜索和多选 |
| 推送 Webhook | 成功后把结果发到哪些飞书群 | 可多选；不需要飞书推送时可不选 |
| 告警 Webhook | 作业失败时通知哪些飞书群 | 建议至少选择一个自己能看到的飞书群 |
| 启用状态 | 这个任务现在是否生效 | 要使用就打开 |

如果选择多个群，系统会把它们放进同一次任务中处理。如果希望每个群分别生成一份日报，建议每个群单独建一个任务。

任务保存后，回到任务列表，点击任务左边的展开按钮，就能看到它下面的作业。

## 十、创建作业：决定什么时候运行、文件放哪里

展开一个任务，点击“新增作业”。

### 1. 基础设置

| 页面上的名称 | 大白话解释 | 建议怎么填 |
|---|---|---|
| 启用 | 这个作业是否会运行 | 配好后打开；调试时也可以先关闭 |
| 作业名称 | 这次执行计划叫什么 | 例如“每天早上生成昨日群日报” |
| 调度类型 | 多久执行一次 | 普通用户选每日、工作日、周末、手动或周报模式 |
| 聊天记录日期基准 | 取今天的内容还是从前一天开始取 | 生成昨天的日报时选“开始时间前一日” |
| 聊天记录开始时间 | 从几点开始读取群聊 | 例如 `00:00` |
| 聊天记录结束时间 | 读到几点为止 | 例如 `24:00` |
| 执行时间 | 系统真正开始工作的时间 | 应晚于聊天结束时间几分钟，例如 `00:10` |
| 偏移分钟数 | 把整个取数时间段前后移动 | 普通用户填 `0` |
| 最大重试次数 | 失败后最多再试几次 | 建议 `1`；填 1 表示失败后再试 1 次 |
| 重试间隔 | 两次尝试之间等多久 | 保持默认值即可 |
| 磁盘写入告警 | 文件写入过大时提醒 | 普通用户建议关闭，不影响日报功能 |
| 备注 | 给自己看的说明 | 可写这个作业的用途 |

### 2. 调度类型怎么选

| 调度类型 | 大白话解释 |
|---|---|
| 每日执行 | 每天运行一次 |
| 工作日执行 | 周一到周五运行 |
| 周末执行 | 周六、周日运行 |
| 自定义周几 | 自己选择每周哪几天运行 |
| 手动执行 | 不自动运行，需要自己点“手动执行” |
| 周报模式 | 按一整周读取群聊并生成周报 |
| 自定义 Cron | 给懂定时表达式的人使用，普通用户不要选 |

如果打开“按间隔执行”，系统会在你设定的时间范围里隔一段时间运行一次。例如 08:00 到 18:00，每 120 分钟运行一次。普通日报一般不需要打开。

### 3. 推荐的每日作业填写方法

如果每天凌晨生成前一天的日报，可以这样填：

- 调度类型：每日执行。
- 聊天记录日期基准：开始时间前一日。
- 开始时间：`00:00`。
- 结束时间：`24:00`。
- 执行时间：`00:10`。

意思是：每天 00:10 开始工作，读取完整的前一天群聊。

### 4. 推荐的周报作业填写方法

- 调度类型：周报模式。
- 执行周几：周一。
- 统计周期：上一周。
- 开始日和时间：周一 `00:00`。
- 结束日和时间：周日 `24:00`。

意思是：每周一整理上一周从周一到周日的全部内容。

### 5. 话题卡片作业的额外设置

| 页面上的名称 | 大白话解释 | 建议 |
|---|---|---|
| 文字布局 | 话题在飞书里分开发还是合在一起 | 第一次选“逐话题” |
| 文字自动合并阈值 | 话题超过多少个后自动合并 | 只有选择“自动”时才需要填，默认 `3` |
| 图片推送 | 是否同时发送卡片图片 | 没配飞书 App ID 和 App Secret 时关闭 |
| 图片布局 | 每个话题一张图，还是合成长图 | 第一次选“单张” |
| 图片自动合集阈值 | 话题超过多少个后合成长图 | 只有选择“自动”时才需要填，默认 `3` |

## 十一、作业里的各种“保存路径”怎么填

“路径”就是文件要放在哪个文件夹里。路径输入框只填文件夹，不要把文件名也写进去。

任务页面本身没有保存路径，这是正常的。任务只管“处理哪个群、生成什么内容”；本地文件路径、GitHub 路径和 ima 同步路径都在任务下面的“作业”里配置。

### 1. 最简单、最推荐的填法

直接填写下面这种短路径：

```text
model_outputs/星火群
```

系统会把它放在项目里的 `backups` 文件夹下。上面的例子实际会保存到：

```text
项目文件夹/backups/model_outputs/星火群
```

推荐按用途填写：

| 用途 | 建议填写的路径 |
|---|---|
| 群消息统计 | `message_reports/群名` |
| 模型返回结果 | `model_outputs/群名` |
| HTML 日报 | `html_reports/群名` |
| ima 自动同步 | 与要同步的文件路径保持一致，例如 `model_outputs/群名` |

### 2. 如果一定要保存到项目外面

可以填写 Windows 的完整路径，例如：

```text
C:\Users\你的用户名\Documents\日报输出\星火群
```

不要照抄“你的用户名”，要换成这台 Windows 电脑的真实用户名。

如果不知道完整路径，最省事的方法是：在文件资源管理器里打开目标文件夹，点击地址栏复制完整路径，再粘贴到输入框。

### 3. 文件命名模板是什么意思

文件命名模板就是告诉系统文件叫什么。花括号里的内容会自动变成真实日期或任务名称。

常用写法：

- `{YYYY-MM-DD}`：执行日期，例如 `2026-07-16`。
- `{week_start}`：周报开始日期。
- `{week_end}`：周报结束日期。
- `{task_name}`：任务名称。
- `{job_name}`：作业名称。

例如：

```text
星火群日报_{YYYY-MM-DD}
```

最后可能变成：

```text
星火群日报_2026-07-16.md
```

### 4. 文件名日期偏移是什么意思

- 填 `0`：使用执行当天的日期。
- 填 `-1`：文件名使用前一天日期。
- 填 `1`：文件名使用后一天日期。

例如凌晨 00:10 生成昨天的日报，通常可以填 `-1`，这样文件名显示昨天的日期。

## 十二、作业的输出选项分别是什么意思

### 1. 导出群消息统计

这个功能统计每个群成员发了多少条消息。

| 配置 | 大白话解释 |
|---|---|
| 导出格式 | `Markdown` 适合阅读，`CSV` 适合表格软件，`Excel` 可直接用 Excel 打开 |
| 保存路径 | 建议填 `message_reports/群名` |
| 文件命名模板 | 建议填 `每日群成员发言数量统计_{YYYY-MM-DD}` |
| 日期偏移 | 统计昨天的数据时通常填 `-1` |

### 2. 备份模型返回结果

这个功能把模型写出的日报另外保存成文件。想同步到 ima 时，必须先打开它。

| 配置 | 大白话解释 |
|---|---|
| 保存路径 | 建议填 `model_outputs/群名` |
| 文件格式 | `Markdown` 适合 ima 和文档阅读；`TXT` 是最简单的纯文字 |
| 文件命名模板 | 建议填 `模型输出_{YYYY-MM-DD}` 或 `群名日报_{YYYY-MM-DD}` |
| 同步到 ima | 打开后，这次生成的文件会继续送到 ima |

如果作业同时开着「本地 HTML 备份」或「GitHub 部署」，模型返回的是完整网页，系统会自动把它转换成可读的 Markdown 再保存和同步（标题、列表、表格、加粗等会保留，网页按钮、导航等界面元素会去掉），ima 里读到的就是干净的文字版；网页原文件仍会照常保存在 HTML 备份和 GitHub 部署里，不受影响。

### 3. 本地 HTML 备份

打开后，系统会让模型生成一个完整网页，并把网页文件保存在本机。

- HTML 备份路径：建议填 `html_reports/群名`。
- HTML 文件命名模板：例如 `群名日报_{YYYY-MM-DD}`。

如果只需要普通文字日报，不需要打开这个功能。

### 4. 部署日报到 GitHub

打开后，系统会生成 HTML 日报并上传到 GitHub。必须先完成后面的“GitHub 管理”配置。

### 5. 把群消息统计同步到 GitHub

这个功能和“部署 HTML 日报到 GitHub”不是一回事。它上传的是群成员发言数量统计，文件格式是 Markdown。

要使用它，先打开“导出群消息统计”，再打开“同步群消息统计到 GitHub”。

| 配置 | 大白话解释 | 建议怎么填 |
|---|---|---|
| GitHub 配置 | 统计文件要上传到哪个仓库 | 选择已经保存好的 GitHub 配置 |
| GitHub_消息统计文件名模板 | 上传后的统计文件叫什么 | 例如 `每日群成员发言数量统计_{YYYY-MM-DD}` |
| 日期参数偏移 | 文件名使用哪一天的日期 | 统计昨天时通常填 `-1` |
| 消息统计根目录 | 在仓库里用哪个一级文件夹区分群 | 例如 `xinghuo`，不要在开头加 `/` |

### 6. 案例卡图片自动备份（默认已开启）

案例卡片任务每次执行后，会自动把本次的素材保存到本机，方便事后排查"拼接错位、一张卡混入两个话题"这类问题：

| 项目 | 说明 |
|---|---|
| 保存位置 | `backups/image_cards/exec_执行编号/` |
| 保存内容 | `half_01.png` 起是每张半卡原图（按内容块顺序编号）、`card_01.png` 起是拼接后的长图、`blocks_source.txt` 是本次的内容块原文 |
| 保留数量 | 默认只保留最近 30 次执行，更早的自动删除 |
| 怎么调整 | 在 `.env` 里改 `TS_IMAGE_CARD_BACKUP_ENABLED`（开/关）、`TS_IMAGE_CARD_BACKUP_DIR`（目录）、`TS_IMAGE_CARD_BACKUP_KEEP_LAST`（保留次数，0 表示全部保留） |

另外，系统现在会在生成图片之前校验内容块的「【拼卡·上】/【拼卡·下】」标记：必须上、下交替且成对。如果模型输出的块有漏写、重复或顺序错乱，任务会直接报错并自动重试，不会再把两个话题的错误内容拼进同一张卡。

系统会按年份和月份自动整理目录，不需要自己提前创建文件夹。

### 6. 高级配置

| 配置 | 大白话解释 | 建议 |
|---|---|---|
| 自定义时间范围 | 无论什么时候执行，都固定读取你指定的那一段日期 | 只用于补跑历史日报；平时留空 |
| 生效日期范围 | 这个作业只在指定日期内允许自动运行 | 临时活动或限时项目才填写；长期日报留空 |
| 偏移分钟数 | 把读取群聊的整段时间一起前后移动 | 普通用户保持 `0` |
| 最大重试次数 | 失败后再试几次 | 建议 `1`，不要设置得太大，避免重复花费模型费用 |
| 重试间隔 | 失败后等多久再试 | 保持默认值 |

### 7. 原始群聊内容备份

系统里有保存完整群聊文字的开关。因为原始群聊可能包含隐私，普通用户建议保持关闭。只做成员发言统计时，使用“数据导出 + 导出群消息统计”即可。

## 十三、GitHub 管理：把 HTML 日报变成网页

GitHub 是可选功能。不需要在线网页时，可以完全不配置。

> **隐私提醒：GitHub Pages 网页可能对互联网公开，即使仓库本身是私有仓库，也不能把它当成私密存储。没有得到群成员允许时，不要把含有私人内容的日报发布成公开网页。**

### 1. 先准备 GitHub 仓库

1. 登录 GitHub。
2. 新建一个专门保存日报的仓库。
3. 仓库至少要有一次提交，确保 `main` 分支已经存在。
4. 进入仓库的 `Settings → Pages`。
5. 在 `Build and deployment` 里选择 `Deploy from a branch`。
6. 分支选择 `main`，文件夹选择 `/(root)`，然后保存。

如果不需要网页，只想把文件备份到 GitHub，可以不打开 Pages。

### 2. 创建 GitHub Token

1. 点击 GitHub 右上角头像，进入 `Settings`。
2. 进入 `Developer settings`。
3. 选择 `Personal access tokens → Fine-grained tokens`。
4. 点击 `Generate new token`。
5. Token 名称可以写“日报系统”。
6. `Repository access` 选择 `Only select repositories`，只勾选日报仓库。
7. 在仓库权限里，把 `Contents` 设为 `Read and write`。
8. `Metadata` 保持只读。
9. 生成 Token 后立即复制保存；页面通常不会再次完整显示。

### 3. 在日报系统里新增 GitHub 配置

进入“GitHub 管理 → GitHub 配置”，点击“新增配置”。

| 页面上的名称 | 大白话解释 | 建议怎么填 |
|---|---|---|
| 配置名称 | 给这套 GitHub 设置起名 | 例如“日报主仓库” |
| GitHub Token | 刚才生成的通行密码 | 原样粘贴 |
| 测试 Token | 检查 Token 是否有效，并列出能访问的仓库 | 填完 Token 后点击 |
| Token 可访问仓库 | 让系统自动填写仓库信息 | 从列表选择日报仓库 |
| 仓库所有者 | GitHub 用户名或组织名 | 选择仓库后通常自动填写 |
| 仓库名称 | 仓库本身的名字 | 选择仓库后通常自动填写 |
| 分支 | 文件上传到哪个分支 | 通常填 `main`，要和 Pages 设置一致 |
| 路径前缀 | 文件放在仓库里的哪个文件夹 | 可填 `reports/星火群`；不想分文件夹就留空 |
| 文件名模板 | 上传后的文件叫什么 | 例如 `微信群日报_{YYYY-MM-DD}.html` |
| Pages 基础链接 | 网页的前半段地址 | 通常自动填写；没有 Pages 可以留空 |
| 查看页面链接 | 执行历史里的“查看页面”打开哪里 | 通常选默认 GitHub Pages 链接 |
| 自定义链接模板 | 使用自有域名时才填写 | 普通用户不要选 |
| 设为默认 | 新作业优先使用这套配置 | 只有一套配置时建议打开 |
| 备注 | 给自己看的说明 | 可不填 |

保存后，在日报任务的作业里打开“部署日报到 GitHub”，选择这套配置，再填写 GitHub 文件名模板。

“GitHub 管理 → 部署记录”可以查看每次上传是否成功、文件上传到了什么位置，以及失败原因。

## 十四、ima 知识库管理

ima 也是可选功能。不需要把日报放进知识库时，可以完全不配置。

### 1. 先在 ima 准备好知识库

1. 登录 [ima](https://ima.qq.com/)。
2. 提前创建好要接收日报的知识库和文件夹。
3. 打开 [ima API 凭证页面](https://ima.qq.com/agent-interface)。
4. 生成或复制 `Client ID` 和 `API Key`。

这两个值都属于账号密钥，不要发给别人。

### 2. 在日报系统里新增 ima 账号

进入“ima知识库管理 → ima账号管理”，点击“新增ima账号”。

| 页面上的名称 | 大白话解释 | 建议怎么填 |
|---|---|---|
| 账号名称 | 给这个 ima 账号起一个好认的名字 | 例如“我的日报知识库” |
| Client ID | ima 凭证页面给出的账号标识 | 原样粘贴 |
| API Key | ima 凭证页面给出的密钥 | 原样粘贴 |
| 备注 | 记录账号用途或密钥到期时间 | 可选 |
| 启用状态 | 这个账号现在是否允许使用 | 打开 |
| 设为默认账号 | 作业没有单独指定时使用它 | 只有一个账号时建议打开 |
| 默认同步目标 | 默认写入 ima 笔记还是 ima 知识库 | 一般选“ima 知识库” |
| 默认笔记本 | 选择写入哪个笔记本 | 只有目标选“ima 笔记”时出现 |
| 默认知识库 | 选择写入哪个知识库 | 目标选“ima 知识库”时必须选择 |
| 默认文件夹 | 选择知识库里的具体文件夹 | 不需要分类时选“根目录” |

填好 Client ID 和 API Key 后：

1. 点击“刷新选项”，让系统加载你的笔记本和知识库。
2. 选择默认知识库和文件夹。
3. 点击“测试连接”。
4. 测试成功后保存。

### 3. 方法一：日报生成后直接同步到 ima

这是最简单的做法。

1. 打开“任务&作业”。
2. 编辑要同步的日报作业。
3. 打开“备份模型返回结果”。
4. 文件格式选择 `Markdown (.md)`。
5. 保存路径例如填写 `model_outputs/星火群`。
6. 打开“同步到 ima”。
7. 打开“使用默认ima账号”和“使用默认 ima 配置”。
8. 保存作业。

以后这个作业每次成功生成模型文件后，都会尝试把文件同步到默认 ima 知识库。ima 同步失败不会把原来的日报执行改成失败。

“同步到 ima”下面各配置的意思如下：

| 页面上的名称 | 大白话解释 | 建议怎么选 |
|---|---|---|
| 使用默认ima账号 | 是否直接使用“ima账号管理”里标成默认的账号 | 只有一个 ima 账号时打开 |
| ima账号 | 当前作业单独使用哪个账号 | 关闭“使用默认ima账号”后才需要选择 |
| 使用默认 ima 配置 | 是否使用该账号预先设置好的默认知识库和文件夹 | 第一次使用建议打开 |
| 同步目标类型 | 写入 ima 笔记还是知识库 | 一般选“ima 知识库” |
| 目标笔记本 | 笔记模式下写入哪个笔记本 | 只有选择“ima 笔记”时出现 |
| 目标知识库 | 当前作业单独写入哪个知识库 | 关闭“使用默认 ima 配置”后选择 |
| 目标文件夹 | 当前作业写入知识库里的哪个文件夹 | 不需要分类时选“根目录” |

### 4. 方法二：定时扫描一个文件夹并同步到 ima

这个方法适合把某个文件夹里的 Markdown 或 TXT 文件统一同步到 ima。

进入“ima知识库管理 → ima知识库同步设置”，点击“新增同步作业”。

| 页面上的名称 | 大白话解释 | 建议怎么填 |
|---|---|---|
| 作业名称 | 这套同步计划叫什么 | 例如“星火日报自动同步” |
| 启用状态 | 是否自动运行 | 配好后打开 |
| ima账号 | 使用哪个 ima 账号 | 选择刚才保存的账号 |
| 同步目标 | 写入笔记还是知识库 | 一般选“ima 知识库” |
| 目标笔记本 | 笔记模式下写入哪里 | 只在选择“ima 笔记”时出现 |
| 目标知识库 | 写入哪个知识库 | 必须选择 |
| 目标文件夹 | 写入知识库里的哪个文件夹 | 可选根目录或具体文件夹 |
| 本地同步路径 | 要扫描哪个本机文件夹 | 要和日报作业的模型输出路径保持一致，例如 `model_outputs/星火群` |
| 是否递归扫描子目录 | 是否连下级文件夹一起扫描 | 文件夹里还有分类目录时打开 |
| 允许同步的文件格式 | 哪些文件可以送进 ima | 支持 Markdown 和 TXT |
| 同步频率 | 每天还是每周扫描一次 | 按需要选择 |
| 每周执行日 | 每周模式下哪天执行 | 只在每周模式出现 |
| 同步时间 | 每天几点开始同步 | 例如 `08:00` |
| 失败告警 Webhook | 同步失败时通知哪个飞书群 | 建议选择管理员能看到的 Webhook |

相对路径都从项目的 `backups` 文件夹开始计算。因此，如果日报作业的模型输出路径是：

```text
model_outputs/星火群
```

ima 同步作业的本地同步路径也填写同样的内容。

“ima知识库管理 → ima同步记录”可以查看每次扫描了多少文件、成功多少、跳过多少、失败多少。系统发现同名内容已经存在时，可能会显示为“跳过”，不一定是错误。

## 十五、第一次创建一套日报的推荐做法

如果只是想先跑通，不要一开始把所有功能都打开。按下面配置最稳妥：

1. WeFlow 测试成功。
2. 新增一个模型，并通过连通性测试。
3. 新增一个飞书 Webhook，先只配置文字推送。
4. 新建一份简单日报提示词。
5. 创建“日报”任务，只选择一个群、一个主模型、一个推送 Webhook。
6. 在任务下面创建“手动执行”作业。
7. 暂时关闭 GitHub、ima、图片推送和磁盘告警。
8. 保存后点击“手动执行”。
9. 到“执行历史”查看结果，并检查飞书群是否收到日报。
10. 主流程成功后，再逐个打开本地文件、GitHub、ima 或图片推送。

一次只增加一个功能。这样如果失败，很容易知道是哪一项配置有问题。

## 十六、执行历史和其他页面怎么看

### 总览

查看任务数量、作业数量、今天执行了几次、WeFlow 是否正常，以及下一次什么时候执行。

### 执行历史

每次手动或自动运行都会留下一条记录。点击“查看详情”可以看到：

- 使用了哪个任务和作业。
- 使用了哪个模型。
- 是否成功。
- 模型返回了什么。
- 生成了哪些文件。
- GitHub 是否上传成功。
- ima 是否同步成功。
- 失败时的错误原因。

### GitHub 部署记录

专门查看 HTML 日报和消息统计上传到 GitHub 的结果。

### ima 同步记录

专门查看哪些文件已经同步、哪些被跳过、哪些失败。

### 告警

集中查看配置错误、执行失败等问题。

### 日志

普通用户平时不需要配置。遇到问题时，可以到“系统设置”下载后端日志，再按文末的提示词让 AI 帮忙判断。发送日志前必须检查并删掉 Token、密钥、群聊内容和个人信息。

## 十七、常见问题

### 1. 浏览器打不开日报系统

先检查一键启动脚本弹出的运行窗口是否还开着。窗口关闭后，日报系统也会停止。重新双击项目最外层的 `启动日报系统.cmd`，然后打开窗口最后显示的日报页面地址。

### 2. 启动窗口提示端口已被占用

这不代表项目坏了。脚本会自动找一个可用端口，并在窗口最后显示新的日报页面地址。不要自己猜地址，直接打开窗口显示的链接即可。

### 3. WeFlow 测试失败

检查 WeFlow 是否打开、API 服务是否打开、地址是否为 `http://127.0.0.1:5031`、Token 前后是否有空格。

### 4. 群聊下拉框是空的

先确认 WeFlow 自己能看到群聊，再确认日报系统里的 WeFlow 测试通过。然后在群聊输入框里输入群名关键词搜索。

### 5. 模型测试失败

最常见的原因是 API Key 错误、模型 ID 不对、接口地址没有写完整，或者请求标准选错。把这四项重新和模型服务商提供的资料逐一对照。

### 6. 飞书没有收到消息

检查作业是否执行成功、任务是否选择了推送 Webhook、Webhook 是否属于目标群，以及飞书机器人的关键词安全规则是否拦截了消息。

### 7. 话题卡片文字能发，图片不能发

检查飞书 App ID、App Secret、机器人能力和“获取与上传图片或文件资源”权限。保存 Webhook 后重新编辑，依次测试图片上传和图片推送。

### 8. 文件不知道保存到哪里了

如果填写的是 `model_outputs/星火群`，就在项目的 `backups/model_outputs/星火群` 里找。

也可以进入“执行历史 → 查看详情”，在“导出文件”里查看实际保存位置。

### 9. GitHub 上传成功，但网页打不开

检查仓库的 `Settings → Pages` 是否选择了正确分支和 `/(root)`。GitHub Pages 第一次生成网页也可能需要等待几分钟。

### 10. ima 连接成功，但没有同步文件

检查作业是否打开“备份模型返回结果”和“同步到 ima”，或者 ima 独立同步作业的本地路径是否和模型输出路径完全一致。

### 11. 一键启动窗口提示缺少 Python、Node.js、Poetry 或 pnpm

脚本会自动补齐缺少的电脑基础运行环境和项目环境依赖。如果自动处理后仍然出现这个提示，不要删除项目，保持错误窗口打开，然后使用下一章的提示词让 AI 一步一步指导。

## 十八、遇到问题时，复制这段话发给 AI

发送前一定要删掉 WeFlow Token、模型 API Key、GitHub Token、ima API Key、飞书 App Secret、聊天记录和其他隐私信息。

```text
我是一个不懂编程的 Windows 用户，正在按照《日报系统使用说明》配置“群聊日报智能助手”。

我现在配置到的章节是：
【例如：配置模型 API / 创建日报作业 / 配置飞书 Webhook】

我填写的配置项名称是：
【只写字段名称，不要粘贴任何 Token、API Key 或 Secret】

我刚才做的操作是：
【用大白话说明点了什么按钮，或者粘贴执行的命令】

页面或一键启动窗口显示的完整错误是：
【粘贴错误；先删掉密钥、群聊内容、邮箱、手机号和真实私人路径】

我现在看到的现象是：
【例如：WeFlow 测试失败、找不到群、飞书没收到消息、ima 没有同步】

请按下面要求帮助我：
1. 先判断最可能的原因，不要直接修改项目代码。
2. 一次只让我做一个步骤，全部使用大白话。
3. 每条命令都告诉我复制到哪里、什么时候按回车。
4. 不要让我删除项目、数据库、配置文件或历史记录。
5. 不要让我执行 rm、git reset、git clean 等删除或清理命令。
6. 不要修改 WeFlow、模型服务商、飞书、GitHub 或 ima 的源码。
7. 不要让我发送 Token、API Key、App Secret、配置文件或聊天记录。
8. 如果需要安装软件或更改系统设置，先解释原因，等我确认后再继续。
9. 每做完一步，先让我告诉你结果，再给下一步。
```

## 十九、安全提醒

- WeFlow Token、模型 API Key、GitHub Token、ima API Key 和飞书 App Secret 都不能发给别人或 AI。
- 不要分享数据库、完整日志、配置文件或包含聊天内容的截图。
- GitHub Pages 可能公开展示网页，上传前先确认内容是否允许公开。
- 日报系统运行时，WeFlow 和一键启动脚本弹出的窗口都要保持打开。
- 不要同时修改很多配置。一次改一项，测试成功后再改下一项。

## 二十、官方参考页面

- [WeFlow HTTP API 说明](https://github.com/hicccc77/WeFlow/blob/main/docs/HTTP-API.md)
- [飞书自定义机器人使用指南](https://open.feishu.cn/document/ukTMukTMukTM/ucTM5YjL3ETO24yNxkjN)
- [飞书上传图片说明](https://open.feishu.cn/document/server-docs/im-v1/image/create)
- [GitHub Token 官方说明](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/managing-your-personal-access-tokens)
- [GitHub Pages 发布来源设置](https://docs.github.com/en/pages/getting-started-with-github-pages/configuring-a-publishing-source-for-your-github-pages-site)
- [ima API 凭证页面](https://ima.qq.com/agent-interface)

## 二十一、本地 HTML 案例卡引擎（零错别字生图）

### 是什么

生图模型画图容易写错别字。本地 HTML 引擎改为：文案 → HTML 排版 → Edge 浏览器截图。文字由浏览器字体引擎渲染，**从机制上不存在错别字**，且改字重出只需几秒。

**接入方式有两种（格式即开关）**：

- **话题卡片任务**（推荐）：任务的提示词输出「内容块格式」（`<!-- CONTENT_BLOCK_START -->` 包裹的 V5/V6 案例卡文案，见 `日报Trial/8.mandao_card_prompt_text_onlyV6_one_card.md`）即自动走 HTML 引擎。无需任何开关，作业里把「图片输出」打开即可；提示词输出 JSON 的话题卡片任务仍走原渲染器，互不影响。图片推送开启时不再重复推文本卡片。
- **图片卡片任务**：在 `.env` 设置 `TS_HTML_CARD_ENGINE_ENABLED=true` 后，跳过生图模型，上/下卡内容块自动配对成一张完整长卡。

- 图片卡片任务不再调用生图模型，也不再需要配置生图模型序列；
- 同一话题的【拼卡·上】+【拼卡·下】两块文案自动合并渲染成**一张完整长卡**（固定宽 1080、高度随内容自适应，默认 2 倍清晰度输出 2160 宽）；
- 卡片版式：报刊杂志风（浅底黑橙），背景概述（头像+事实字段+场景+入局问句）、人物关系网络图（当事人居中、箭头骑动词标签、无形角色虚线框）、分析过程（圆徽章编号）、解决方案（大数字编号）、金句、页脚（「漫道」职场群口号 + 「喜欢您来」 + 入群二维码）；
- 推送、本地备份、执行记录等后续流程与原链路完全一致。

### 头像素材

放 `image/` 文件夹（可用 `TS_HTML_CARD_AVATAR_DIR` 改路径）：**女性头像命名 F1、F2、…，男性头像命名 M1、M2、…**（jpg/png/webp 均可）。引擎按文案里的「IP角色：男/女」选图，同一次执行的多张卡会轮换不同头像。直接往文件夹里加图即可，无需改代码。

### 人物关系图不准时，手动修正

关系图是按关系句自动布局的，个别话题可能摆得不好看。修正流程：

1. 打开本次执行的卡片目录：`backups/html_cards/<时间戳-任务名>/<序号>/card.html`（双击可在浏览器预览）；
2. 用任意编辑器改 `card.html`：
   - **挪节点**：找 `<div class="dg-node …" style="left:…px;top:…px;…">`，改 left/top（节点左上角坐标）；
   - **改连线**：找 `<svg>` 里的 `<line x1="…" y1="…" x2="…" y2="…">`，改四个端点坐标；
   - **挪动词标签**：找 `<div class="dg-verb" style="left:…px;top:…px">`，改 left/top；
**可视化编辑（推荐，不想碰代码用这个）**：把卡片文件夹拖到项目根目录的 **`可视化改卡.cmd`** 上（或 `poetry run python scripts/card_editor.py <卡片文件夹>`），浏览器会打开可编辑的卡片页——**鼠标拖动**关系图节点和动词标签（连线自动跟随）、**双击任意文字**直接改内容，改完点右上角**「保存并出图」**：自动备份原文件为 card.html.bak、写回修改并重新生成 card.png。改完关闭命令行窗口即可。

3. 重新出图（只截图，**不会改动你手改的 HTML**）：
   ```
   poetry run python scripts/render_html_card.py render backups/html_cards/<目录>/<序号>
   ```
4. 改完的 `card.png` 直接拿去用，或重新触发推送。

如果改的是文案本身（`card_data.json`），用 `build` 子命令重建（会重新生成 card.html）：
```
poetry run python scripts/render_html_card.py build backups/html_cards/<目录>/<序号>/card_data.json
```

### 相关配置（.env）

| 配置 | 默认值 | 说明 |
|---|---|---|
| TS_HTML_CARD_ENGINE_ENABLED | false | 开关：true=本地渲染，false=生图模型 |
| TS_HTML_CARD_OUTPUT_DIR | data/html_cards | 卡片产物持久化目录 |
| TS_HTML_CARD_JOIN_URL | https://md.xinjianhub.cn/#join-us | 页脚二维码指向 |
| TS_HTML_CARD_AVATAR_DIR | image | 头像素材目录（F*/M* 命名） |
| TS_HTML_CARD_EDGE_PATH | 留空自动探测 | Edge/Chrome 路径 |
| TS_HTML_CARD_RENDER_SCALE | 2 | 输出倍率 |
| TS_HTML_CARD_ARROW_STYLE | curve | 关系图箭头：curve=智能混合（辐射线弧线、卫星线直线）/ solid=全部直线大三角 |

注意：开启本地引擎后，`TS_QR_CODE_*` 的叠加逻辑不再参与（二维码已直接排版进页脚），作业里的「图片比例/分辨率」选项也不再约束本引擎（宽度固定 1080、高度自适应）。
